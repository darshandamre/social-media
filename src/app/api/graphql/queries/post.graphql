query Post($postId: String!) {
  post(id: $postId) {
    id
    content
    isLikedByMe
    isBookmarkedByMe
    authorId
    author {
      name
      username
    }
    createdAt
    updatedAt
  }
}
