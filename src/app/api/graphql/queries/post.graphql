query Post($postId: String!) {
  post(id: $postId) {
    id
    content
    likes
    isLikedByMe
    isBookmarkedByMe
    authorId
    author {
      name
      username
    }
    createdAt
    updatedAt
  }
}
