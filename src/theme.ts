import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    background: { default: "#181a1f" },
    primary: {
      main: "#6DD5FA"
    },
    text: {
      primary: "#fff"
    }
  }
});
